# CompareFramework CI fixture

`CompareFramework_CI.ods` is the minimal Calc document used by the D2-04.1 UNO harness. It must remain free of an embedded CompareFramework monolith; the current monolith is injected dynamically at runtime.
